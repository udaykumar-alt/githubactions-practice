name: concurrency
on: push
concurrency:
  group: mbapp
  cancel-in-progress: false
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: concurrencyyy
        run: |
          echo "Today's date is::"
          date
          sleep 38